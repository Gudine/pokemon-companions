{
  "name": "pokemon-individuals",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "data:populate": "node scripts/getPokemonList.js",
    "sprites:populate": "node scripts/getPokemonSprites.js",
    "populate": "pnpm run data:populate && pnpm run sprites:populate",
    "preview": "vite preview"
  },
  "dependencies": {
    "preact": "^10.19.3"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.8.1",
    "node-fetch": "^3.3.2",
    "typescript": "^5.2.2",
    "vite": "^5.1.0"
  }
}
